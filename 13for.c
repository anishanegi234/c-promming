#include <stdio.h>

int main() {
    int i;

    // Using for loop to print the first 5 even numbers
    for (i = 1; i <= 5; i++) {
        printf("%d\n", i * 2);  // Multiply i by 2 to get the even number
    }

    return 0;
}
