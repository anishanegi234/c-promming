#include <stdio.h>

int main() {
    int i = 1;

    // Using do-while loop to print numbers from 1 to 5
    do {
        printf("%d\n", i);
        i++;  // Increment i by 1
    } while (i <= 5);

    return 0;
}
